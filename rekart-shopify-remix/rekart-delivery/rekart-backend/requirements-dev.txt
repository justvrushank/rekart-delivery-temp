# Test-only dependencies (install with: pip install -r requirements-dev.txt)
# pytest + pytest-asyncio are pinned in requirements.txt; this adds the async
# SQLite driver used by the hermetic test suite.
aiosqlite==0.20.0
